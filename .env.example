PORT=4000
DATABASE_PATH=./data/schlussel.db
KEYS_DIR=./data/keys
JWT_ISSUER=schlussel

# Comma-separated list of allowed CORS origins - include kuvert's and
# Zettel's dev servers too (not just schloss's), they call /theme
# directly for cross-origin theme sync
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5176,https://schloss.example.com,https://zettel.example.com
