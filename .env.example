PORT=4000
DATABASE_PATH=./data/schlussel.db
KEYS_DIR=./data/keys
JWT_ISSUER=schlussel

# Comma-separated CORS origins. Include every separate-origin frontend that
# calls the public /theme endpoint directly for theme sync.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,https://schloss.example.com,https://kuvert.example.com,https://tafel.example.com,https://zettel.example.com
